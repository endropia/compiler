var
	i: integer;
	a: array[0..100] of Integer;
	b: record x, y: integer; end;
begin
	for i := 0 to 10 do begin end;
	for i := 3 * 6 + 7 to (3 - 2) - 3 do begin end;
	for a[0] := 0 to 10 do begin end;
	for b.x := 0 to 10 do begin end;

	while (i < 10) do begin
    	i += 1;
    end;

end.